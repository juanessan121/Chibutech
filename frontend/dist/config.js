// ─────────────────────────────────────────────────────────────────────────
// URL del backend (API de Chibutech).
//
// Este archivo NO se compila junto con el resto del código: se copia tal
// cual a la carpeta final (dist/) y se puede editar directamente en el
// servidor (cPanel → Administrador de Archivos → Editar) sin necesidad de
// instalar Node.js, npm, ni volver a ejecutar "npm run build".
//
// Cambia solo el valor de abajo por la URL real de tu backend en producción,
// por ejemplo: 'https://api.mi-dominio.com/api'
// Guarda el archivo y recarga la página (Ctrl+F5) — no hace falta nada más.
// ─────────────────────────────────────────────────────────────────────────
window.__API_BASE_URL__ = 'http://localhost:8080/api';
